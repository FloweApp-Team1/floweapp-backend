{
  "$schema": "http://json.schemastore.org/launchsettings.json",
  "profiles": {
    // The gateway has no UI of its own - hit /health to confirm it is up, then call the
    // services through /api/identity/... and /api/catalog/...
    "http": {
      "commandName": "Project",
      "dotnetRunMessages": true,
      "launchBrowser": false,
      "applicationUrl": "http://localhost:5235",
      "environmentVariables": {
        "ASPNETCORE_ENVIRONMENT": "Development"
      }
    }
  }
}
