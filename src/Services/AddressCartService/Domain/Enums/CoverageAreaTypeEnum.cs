namespace AddressCartService.Domain.Enums
{
    // Mirrors the CoverageAreaType schema in the API contract.
    public enum CoverageAreaTypeEnum
    {
        Polygon,
        Radius,
        CityAreaList
    }
}
